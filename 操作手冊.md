# PMPS 系統操作手冊

本手冊涵蓋 PMPS 系統的日常維運操作、部署更新流程，以及核心功能設定的相關說明。

---

## 1. 系統更新與部署 (Ubuntu Docker)

當 GitHub 上的程式碼有更新時，請按照以下步驟在您的 Ubuntu 伺服器上拉取最新程式碼並重新啟動系統。

### 1.1 完整更新流程 (純 Docker)

如果您是透過原生的 `docker run` 啟動容器，請執行：

```bash
# 1. 停止並移除正在運行的舊 Container (假設名稱為 pmps-app)
docker stop pmps-app
docker rm pmps-app

# 2. 拉取最新程式碼
# (若出現密碼提示，請貼上您的 GitHub Personal Access Token)
git pull origin main

# 3. 重新建立 Docker Image
docker build -t pmps-image .

# 4. 重新啟動 Container (請替換為您當初設定的 port)
docker run -d --name pmps-app -p 80:3000 pmps-image
```

### 1.2 完整更新流程 (Docker Compose)

如果您是使用 `docker-compose` 來管理：

```bash
# 1. 停止並移除舊的 Container
docker compose down

# 2. 拉取最新程式碼
git pull origin main

# 3. 重新構建並啟動
docker compose up -d --build
```

---

## 2. GitHub Personal Access Token (PAT) 產生與使用

由於 GitHub 已經停止支援直接使用密碼進行命令列驗證，您必須使用 PAT 才能在 Ubuntu 上執行 `git pull`。

> **⚠️ 注意**：PAT 在產生後**只會顯示一次**，基於安全性，之後就無法再次查看。如果遺失，請直接刪除舊的並重新產生。

### 產生步驟：
1. 登入 GitHub，點選右上方個人大頭貼，選擇 **Settings**。
2. 左側選單滑到最下方，點選 **Developer settings**。
3. 展開 **Personal access tokens**，選擇 **Tokens (classic)**。
4. 點選右上角的 **Generate new token (classic)**。
5. 在 **Note** 欄位填寫用途（例如：`Ubuntu-Deploy`）。
6. **Expiration** 選擇 Token 有效期限（例如 `No expiration` 或 1 年）。
7. **Scopes (權限)** 區塊中，**請務必勾選 `repo`** (Full control of private repositories)。
8. 點擊 **Generate token**，並**立刻將畫面上的 `ghp_...` 亂碼字串複製並妥善保存**。

**使用方式**：
在 Ubuntu 執行 `git pull` 時，當系統提示輸入密碼 (Password)，請貼上這串 PAT。

---

## 3. 部門與主管權限管理

系統支援讓特定主管（Manager）跨部門查看資料，而不必給予全域的 Admin 權限。

### 跨部門權限設定步驟：
1. 以 **Admin** 身分登入系統。
2. 進入上方選單的 **使用者管理 (User Management)**。
3. 找到需要設定的主管帳號，點擊編輯 (✏️)。
4. 在 **「管理的部門 (Managed Departments)」** 欄位中，輸入該主管負責的所有部門代碼（按下 Enter 分隔，例如：`IE0C00`、`IE0C30`）。
5. 點擊儲存。該主管登入後，在看儀表板或專案時，就只能看到被指派的這些部門資料。
6. 若該主管只需看單一部門，可留空「管理的部門」，系統會預設只顯示該使用者本身所屬的單一部門（由「所屬部門」欄位決定）。

---

## 4. 利潤中心公式與部門 KPI 目標設定

系統允許您定義全公司的預設 KPI 目標，同時也支援針對「個別部門」設定專屬的 KPI 業績目標。

### 設定步驟：
1. 以 **Admin** 身分登入系統。
2. 進入上方選單的 **利潤中心公式 (Profit Center Formula)**。
3. 您可以先在畫面上半部設定全域的預設參數，例如：「管銷費用分攤率」、「協銷單價」、「年度部門 KPI 目標金額」等。
4. **部門專屬目標設定 (Department Override)**：
   - 在畫面下方的「各部門個別 KPI 目標」區塊。
   - 從下拉選單中選擇特定的部門（例如：`IE0C00`）。
   - 輸入該部門專屬的業績目標金額。
   - 點擊「新增 / 更新」。
5. **儲存設定**：點擊右上角的「儲存設定」按鈕。
6. 設定完成後，**KPI 儀表板 (KPI Dashboard)** 在計算各部門的「達成率 (Achievement Rate)」與「差距 (Gap)」時，會優先套用您剛設定的部門專屬目標。若未特別設定專屬目標的部門，則會退回使用全域的「年度部門 KPI 目標金額」。
